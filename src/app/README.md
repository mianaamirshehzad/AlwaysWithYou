This folder is for app-level (non-feature) code:

- `navigation/`: navigation helpers & route utilities (Expo Router routes still live in the root `app/` directory)
- `providers/`: global providers (React Query, theme, etc.)
- `config/`: app config (env parsing, feature flags)

